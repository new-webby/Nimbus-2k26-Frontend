// SignupScreen is kept as a named alias of LoginScreen so that any
// Navigator.pushNamed(context, '/signup') or existing references compile.
// The actual UI is unified — ClerkAuthentication handles both flows.
export 'login_screen.dart';
